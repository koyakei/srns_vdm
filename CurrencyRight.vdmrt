class CurrencyRight
types

 	
values

instance variables
	public currencyId : nat;
	public user : User;
	public quantity : Quantity;
	public size :nat;
	
operations
public CurrencyRight: nat * User * Quantity ==> CurrencyRight
CurrencyRight(cId, usr, qy) ==
(
	currencyId := cId;
	user := usr;
	quantity := qy
	);
public UpdateSize : nat ==> ()
UpdateSize(s) ==
	size := size + s
functions

traces
-- TODO Define Combinatorial Test Traces here
end CurrencyRight